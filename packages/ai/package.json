{
  "name": "@aide/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI and LangGraph integration for AIDE",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Lint not configured yet'"
  },
  "dependencies": {
    "@aide/shared": "workspace:*",
    "@langchain/langgraph": "^0.0.20"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
